#include<stdio.h>
int main()
{
    int i,j;
    printf("Enter the number to be check:-");
    scanf("%d",&i);
    if(i>0)
    {
        printf("The number is positive");
    }
    else if(i>0)
    {
        printf("The number is negative");
    }
    else
    printf("Zero");
return 0;
}
